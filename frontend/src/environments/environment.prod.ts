export const environment = {
  production: true,
  apiUrl: 'https://budgetking-backend.onrender.com',
  // Google OAuth2 client id (Google Cloud Console -> Credentials -> OAuth client ID -> Web application).
  googleClientId: '',
  enableLocalAuth: false,
};
