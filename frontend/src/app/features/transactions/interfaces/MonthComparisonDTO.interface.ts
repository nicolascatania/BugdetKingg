/** Current calendar month next to the previous one, for the home page comparison. */
export interface MonthComparisonDTO {
  currentIncome: number;
  currentExpense: number;
  previousIncome: number;
  previousExpense: number;
}
