# 🔀 Flujos de Reactividad - Diagramas Visuales

## 1. Flujo Básico: Signal + Effect

```
┌──────────────────────────────────────────────────────────────────┐
│                        CICLO REACTIVO                             │
└──────────────────────────────────────────────────────────────────┘

1. INICIALIZACIÓN
┌─────────────────────────┐
│   Crear Signal          │
│   loadTrigger = signal(0)│
└────────────┬────────────┘
             │
             ↓
┌─────────────────────────────────────────┐
│   Registrar Effect                      │
│   effect(() => {                        │
│     loadTrigger();                      │
│     loadCategories();                   │
│   })                                    │
└────────────┬────────────────────────────┘
             │
             ↓
2. TRIGGER DEL USUARIO
┌─────────────────────────────────────────┐
│   Button click: onPageChange(2)         │
└────────────┬────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────┐
│   loadTrigger.update(v => v + 1)        │
│   // Cambia de 0 a 1                    │
└────────────┬────────────────────────────┘
             │
             ↓
3. ✨ MAGIC: EFFECT DISPARA
┌─────────────────────────────────────────┐
│   Angular detecta: loadTrigger cambió   │
│   → Automáticamente ejecuta effect()    │
└────────────┬────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────┐
│   loadCategories()                      │
│   - Hace HTTP request                   │
│   - Recibe datos                        │
│   - categories.set(data)                │
└────────────┬────────────────────────────┘
             │
             ↓
4. ACTUALIZAR STATE
┌─────────────────────────────────────────┐
│   categories = signal<CategoryDTO[]>([])│
│   categories.set([cat1, cat2, ...])     │
│   // Cambia de [] a [cat1, cat2, ...]   │
└────────────┬────────────────────────────┘
             │
             ↓
5. ✨ TEMPLATE ACTUALIZA
┌─────────────────────────────────────────┐
│   @for (cat of categories(); ...)       │
│   Angular detecta cambio en categories()│
│   → Template re-renderiza               │
│   → Usuario ve nuevos datos             │
└─────────────────────────────────────────┘
```

**Timeline: 0ms → 50ms**
```
t=0ms    → Usuario hace click
t=5ms    → loadTrigger cambia
t=10ms   → Effect se ejecuta
t=15ms   → HTTP request enviado
t=45ms   → HTTP response recibida
t=50ms   → categories.set() actualiza
t=55ms   → Template re-renderiza
t=60ms   → Usuario ve cambios
```

---

## 2. Flujo de Sincronización: Servicio → Componente

```
┌────────────────────────────────────────────────────────────────┐
│                    EDITMODAL CREA CUENTA                        │
└────────────────────────────────────────────────────────────────┘

[EditAccountModal Component]
    │
    ├─ User clicks "Save"
    │
    ↓
    submit() {
      accountService.create(account)
    }


[AccountService]
    │
    ├─ create(account)
    │  └─ super.create() → HTTP POST
    │
    ├─ RefreshableCrudService.wrapWithRefresh()
    │  └─ .pipe(tap(() => this.notifyRefresh()))
    │
    ↓
    refreshSignal.update(v => v + 1)  ← ✨ NOTIFICA


┌────────────────────────────────────────────────────────────────┐
│              NOTIFICACIÓN DISPARA EFFECTS                       │
└────────────────────────────────────────────────────────────────┘

        refresh$ Signal cambió
                │
                ├─ [Home Component]
                │   └─ effect(() => {
                │       this.accountService.refresh$();
                │       this.accountService.loadAccounts();  ← RECARGA
                │     })
                │
                ├─ [AccountList Component]
                │   └─ effect(() => {
                │       this.accountService.refresh$();
                │       this.accountService.loadAccounts();  ← RECARGA
                │     })
                │
                └─ [AnyOther Component]
                    └─ effect(() => {
                        this.accountService.refresh$();
                        // Custom logic...
                      })


[AccountService]
    │
    ├─ loadAccounts()
    │  └─ HTTP GET /account/by-user
    │
    ↓
    _accounts.set(newData)  ← ✨ ACTUALIZA SIGNAL


┌────────────────────────────────────────────────────────────────┐
│           SIGNALS SE ACTUALIZAN → TEMPLATE RE-RENDERIZA          │
└────────────────────────────────────────────────────────────────┘

[Home Component]
    accounts = this.accountService.accounts  ← Signal
        │
        ├─ [Heading Component]
        │   totalBalance = computed(...)  ← Se recalcula
        │       @if (totalBalance() > 0) {...}  ← Template actualiza
        │
        └─ [Accounts Component]
            accounts = this.accountService.accounts  ← Signal
                @for (acc of accounts(); ...) {...}  ← Template actualiza
                    ↓
                    Usuario ve la nueva cuenta
                    ✅ Sin F5, automático!
```

---

## 3. Flujo de Paginación

```
┌────────────────────────────────────────────────────────────────┐
│               PAGINACIÓN REACTIVA                               │
└────────────────────────────────────────────────────────────────┘

UI STATE
┌────────────────────────┐
│ categories = signal([])│
│ paginationState = {...}│
│ loadTrigger = signal(0)│
└────────────┬───────────┘
             │
             ↓
CONSTRUCTOR - Registrar Effect
┌────────────────────────────────────────┐
│ effect(() => {                         │
│   this.loadTrigger();    ← Dependencia │
│   this.loadCategories();               │
│ })                                     │
└────────────┬───────────────────────────┘
             │
             ↓
INITIAL LOAD - ngOnInit()
┌────────────────────────────────────────┐
│ loadTrigger.update(v => v + 1)         │
│ // 0 → 1                               │
└────────────┬───────────────────────────┘
             │
             ↓
✨ EFFECT EJECUTA
┌────────────────────────────────────────┐
│ Angular detecta: loadTrigger cambió    │
│ → ejecuta loadCategories()             │
│ → HTTP GET page=0, size=20             │
│ → categories.set([...])                │
│ → Template renderiza                   │
└────────────┬───────────────────────────┘
             │
             ↓
USER CLICKS PAGE 2
┌────────────────────────────────────────┐
│ onPageChange(2) {                      │
│   paginationState.goToPage(2)          │
│   loadTrigger.update(v => v + 1)       │
│   // 1 → 2                             │
│ }                                      │
└────────────┬───────────────────────────┘
             │
             ↓
✨ EFFECT EJECUTA (AGAIN)
┌────────────────────────────────────────┐
│ Angular detecta: loadTrigger cambió    │
│ → ejecuta loadCategories()             │
│ → HTTP GET page=1, size=20    ← PÁGINA 2 │
│ → categories.set([...])  ← NUEVOS DATOS│
│ → Template renderiza con PAGE 2        │
└────────────────────────────────────────┘


VENTAJA:
┌─────────────────────────────────────┐
│ onPageChange() es SIMPLE:           │
│ ─ paginationState.goToPage(2)       │
│ ─ loadTrigger.update(v => v + 1)    │
│                                     │
│ No necesita:                        │
│ ✘ Llamar a loadCategories()         │
│ ✘ markForCheck()                    │
│ ✘ unsubscribe/subscribe             │
│                                     │
│ Effect se encarga de TODO ✅        │
└─────────────────────────────────────┘
```

---

## 4. Flujo de Búsqueda en Tiempo Real

```
┌────────────────────────────────────────────────────────────────┐
│          BÚSQUEDA CON DEBOUNCE Y FILTER                         │
└────────────────────────────────────────────────────────────────┘

UI COMPONENTE
┌─────────────────────────────────────┐
│ searchQuery = signal('');            │
│ filterType = signal('all');          │
│ searchTrigger = signal(0);           │
└────────────┬────────────────────────┘
             │
             ↓
OBSERVABLE REACTIVO
┌─────────────────────────────────────────────────┐
│ search$ = combineLatest([                       │
│   this.searchQuery,      ← Signal 1              │
│   this.filterType,       ← Signal 2              │
│   this.searchTrigger     ← Signal 3              │
│ ]).pipe(                                        │
│   debounceTime(300),                            │
│   switchMap(([query, type, _]) =>               │
│     this.api.search(query, type)                │
│   ),                                            │
│   catchError(() => of([]))                      │
│ )                                               │
└────────────┬────────────────────────────────────┘
             │
             ↓
CONVERTIR A SIGNAL
┌─────────────────────────────────────┐
│ results = toSignal(search$, {        │
│   initialValue: []                  │
│ })                                  │
└────────────┬────────────────────────┘
             │
             ↓
USUARIO ESCRIBE EN INPUT
┌─────────────────────────────────────┐
│ onSearchChange('angular') {          │
│   searchQuery.set('angular')         │
│   // ↓ Signal cambió                 │
│ }                                   │
└────────────┬────────────────────────┘
             │
             ↓ (cada keystroke)
EMITE ACTUALIZACIÓN
100ms: 'a'       ← debounceTime ignora
150ms: 'an'      ← debounceTime ignora
200ms: 'ang'     ← debounceTime ignora
250ms: 'angu'    ← debounceTime ignora
300ms: 'angul'   ← DEBOUNCE! Envía request


FLUJO DETALLADO
┌────────────────────────────────────────────┐
│ searchQuery = 'angul'                       │
│ ← Cambia                                   │
│   ↓                                        │
│ combineLatest emite ([                     │
│   'angul',    ← searchQuery                │
│   'all',      ← filterType (no cambió)    │
│   0            ← searchTrigger (no cambió)│
│ ])                                        │
│   ↓                                        │
│ debounceTime(300)  ← Espera 300ms        │
│   ↓                                        │
│ Si hay más cambios, reinicia el timer     │
│ Si pasan 300ms sin cambios → emite       │
│   ↓                                        │
│ switchMap:                                │
│   - Cancela request anterior (si existe)  │
│   - Envía nuevo: GET /search?q=angul&... │
│   ↓                                        │
│ Recibe respuesta → results = [result1, ...]│
│   ↓                                        │
│ Template: @for (r of results(); ...) {}  │
│   ↓                                        │
│ Usuario ve resultados                     │
└────────────────────────────────────────────┘


VENTAJA: switchMap()
┌────────────────────────────────────┐
│ Si usuario escribe rápido:          │
│                                    │
│ Request 1: 'a'   ← cancelado       │
│ Request 2: 'an'  ← cancelado       │
│ Request 3: 'ang' ← cancelado       │
│ Request 4: 'angu' ← esperando...   │
│ Request 5: 'angul' ← ENVIADO       │
│            → recibe respuesta       │
│                                    │
│ ✅ Solo una request al final      │
│ ❌ No 5 requests innecesarias     │
└────────────────────────────────────┘
```

---

## 5. Flujo de Creación + Actualización en Cascada

```
┌─────────────────────────────────────────────────────────────────┐
│        CREAR CUENTA → ACTUALIZAR HOME, LIST, DASHBOARD           │
└─────────────────────────────────────────────────────────────────┘

HOME PAGE
┌──────────────────────┐
│ [Heading]            │
│ Total: $5000         │  ← totalBalance (computed)
│ [Accounts List]      │
│ - Account 1: $3000   │
│ [Create Button] ↓    │
└──────────────────────┘


ACCOUNT PAGE
┌──────────────────────┐
│ [Account List Table] │
│ - Account 1: $3000   │
│ [Create Button] ↓    │
└──────────────────────┘


STEP 1: CREAR NUEVA CUENTA
┌──────────────────────────────────────────────┐
│ Modal.submit() {                             │
│   accountService.create({                    │
│     name: 'New Account',                     │
│     balance: $2000                           │
│   })                                         │
│ }                                            │
└────────────┬─────────────────────────────────┘
             │
             ↓
STEP 2: SERVICIO NOTIFICA
┌──────────────────────────────────────────────┐
│ AccountService.create() {                    │
│   return refreshable.wrapWithRefresh(        │
│     super.create(account)                    │
│   )                                          │
│ }                                            │
│ → .pipe(tap(() => notifyRefresh()))          │
│ → refreshSignal.update(v => v + 1)           │
└────────────┬─────────────────────────────────┘
             │
             ↓
STEP 3: CASCADA DE EFFECTS
┌────────────────────────────────────────────────────────┐
│ Todos los components que escuchan refresh$ disparan:  │
│                                                       │
│ [HOME COMPONENT] effect()                             │
│ → accountService.refresh$() ← CAMBIÒ                  │
│ → accountService.loadAccounts()                       │
│   ↓ HTTP GET /account/by-user                         │
│   ↓ Recibe: [Old Acc, New Acc]                        │
│   ↓ _accounts.set([...])                              │
│   ↓ ✨ accounts signal actualizada                    │
│                                                       │
│ [ACCOUNT LIST COMPONENT] effect()                     │
│ → accountService.refresh$() ← CAMBIÒ                  │
│ → accountService.loadAccounts()                       │
│   ↓ HTTP GET /account/by-user                         │
│   ↓ Recibe: [Old Acc, New Acc]                        │
│   ↓ accounts.set([...])                               │
│   ↓ ✨ Tabla se actualiza                             │
│                                                       │
│ [DASHBOARD COMPONENT] effect()                        │
│ → Si escucha también...                               │
│ → loadAccounts()                                      │
│ → tablero se actualiza                                │
└────────────┬───────────────────────────────────────────┘
             │
             ↓
STEP 4: TEMPLATES SE ACTUALIZAN
┌───────────────────────────────────────────────────────┐
│ HOME:                                                 │
│ [Heading]                                             │
│ Total: $7000  ← computed recalculó                   │
│ [Accounts List]                                       │
│ - Account 1: $3000  ← visto aquí                     │
│ - Account 2: $2000  ← ¡¡ NUEVA CUENTA !!             │
│                                                       │
│ ACCOUNT LIST:                                         │
│ [Table]                                               │
│ - Account 1: $3000  ← visto aquí                     │
│ - Account 2: $2000  ← ¡¡ NUEVA CUENTA !!             │
│                                                       │
│ ✅ SIN F5!                                           │
│ ✅ AUTOMÁTICO!                                        │
│ ✅ SINCRONIZADO!                                      │
└───────────────────────────────────────────────────────┘


TIMELINE:
t=0ms    Usuario hace click "Create"
t=50ms   Modal valida forma
t=100ms  HTTP POST /account
t=200ms  Servidor responde ✓
t=210ms  refreshSignal.update()
t=220ms  Effects se activan
t=230ms  HTTP GET /account/by-user (2 requests en paralelo)
t=280ms  Respuestas recibidas
t=290ms  _accounts.set() y accounts.set()
t=300ms  HOME template re-renderiza
t=310ms  ACCOUNT LIST template re-renderiza
t=320ms  Usuario ve cambios

TOTAL: 320ms para ver cambios en múltiples páginas ✅
```

---

## 6. Comparación: CON vs SIN Signals

```
CON SIGNALS (Actual)

User Action
    ↓
Signal actualiza
    ↓
Effect se activa (automático)
    ↓
Servicio actúa
    ↓
Signal actualiza (automático)
    ↓
Template re-renderiza (automático)
    ↓
Usuario ve cambios

Líneas de código: ~50 ✅
Memory leaks: 0 ✅
Sincronización: Automática ✅


SIN SIGNALS (Antiguo)

User Action
    ↓
Manual call a loadData()
    ↓
Subscription + callback
    ↓
Actualizar this.data = response
    ↓
Llamar markForCheck()
    ↓
Template re-renderiza (después de detector)
    ↓
Acordarse de unsubscribe en ngOnDestroy
    ↓
Usuario ve cambios (posible delay)

Líneas de código: ~100 ❌
Memory leaks: Alto riesgo ❌
Sincronización: Manual ❌
```

