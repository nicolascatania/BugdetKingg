1. Budget planner, divide % for categories... alerts when reaching the limits. Like 10% for social, 15% for to spend on what i want (choose categories plus others) 50% for food and education (categories for that) etc.
	1st step modal with categories to choose
	2nd step configure with some bars or something cool percentage levels
	3rd step go to another view to see how much money acorrding to a monthly income i can spend (aprox)
		this will be adapted to new incomes. high
2. Connect to browsers voice api. high
3. DONE. CSV export medium
4. Skeleton screens DONE
5. Input masking for ammounts inputs. low
6. Savings view or some way to tell the user you saved this money last month, or you spent less money than last month. high
7. Streak badge. low
8. Auto categorizing low
9. DONE (savings goals, see docs/features/savings-goals.md). Goals ... Set a goal that has certain money to be acomplished and a progress bar... low
10. DONE (Google-only, see docs/features/google-oauth2-login.md). OAuth2 login to replace the internal user/password management. Check whether it also gives us email verification for free (most providers confirm the email themselves, so we might not need to build that ourselves). high
11. DONE (en/es/de/pl, Transloco). i18n system (English + Spanish), do this after OAuth2 is settled. high
	IMPORTANT: right when the account is created, detect the browser language (navigator.language) and set it as the user's default language; fall back to English if the browser language isn't supported.
12. DONE. Settings view: language switch + light/dark mode toggle. Do this after i18n is in place (needs the language switch to hook into). Will grow with more options later (notifications, currency, etc). medium
13. WhatsApp bot to auto-add transactions. User sends a voice note or short text like "spent X on account Y, description, at merchant Z" and it creates the transaction. high
	Needs a queue + outbox pattern: outbox table written in the same DB transaction as the domain change, a consumer polls/reads it and calls the WhatsApp API — survives retries and avoids losing messages if the API call fails.
	Voice notes need a speech-to-text step first; then an NLP/parsing step to extract account, amount, description, type and counterparty from the free text (or transcript).
	OPEN QUESTION: microservice or integrated into the monolith? Weigh shared domain coupling (needs Transaction/Account/Category from the monolith) against independent scaling/deploys (webhook traffic, WhatsApp API rate limits, retry storms shouldn't affect the main app). Leaning monolith first, extract later if it proves noisy — but decide before building.
14. Multi-currency: currency per account, base currency per user for reports, FX provider. See docs/proposals/multi-currency.md. medium
15. DONE. CSV export/import: write dates in a human-readable format instead of ISO. low
16. DONE. Transaction CSV import: create missing categories on the fly (currently unknown category names are errors). medium
17. DONE. Category detail modal: clicking a category opens a modal with its expenses for the current month and its all-time total. medium
18. DONE. Recurring budgets: a budget that carries over month to month (today one row per category per month; a fixed monthly limit means re-creating it every month). high
19. Versioning: release tagging / version shown in the app, changelog. low
